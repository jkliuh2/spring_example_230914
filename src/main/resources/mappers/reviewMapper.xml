<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.example.lesson03.mapper.ReviewMapper"><!-- 어느 인터페이스와 연결 -->
 
 	<select id="selectReview" resultType="com.example.lesson03.domain.Review"><!-- id=인터페이스 메소드, resultType=하나에 대한 타입의 경로 -->
 		SELECT
 			`id`
			, `storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
			, `createdAt`
			, `updatedAt`
		FROM
			`new_review`
		WHERE
			`id` = 5
			<!-- id=5 딱 한건 -->
 	</select>
 	
 </mapper>